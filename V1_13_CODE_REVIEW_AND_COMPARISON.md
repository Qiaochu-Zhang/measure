# V1_13 代码审查及与 V13_modified 的对比

审查日期：2026-09-18。仓库基线：`e51945d`。本报告检查原版 `V1_13`，并与此前检查的 `V13_modified` 比较；下文简称“原版”和“修改版”。

## 1. 结论

**对你的 CD、LER、LWR 用途，修改版更适合作为继续开发的基础，但不能因此认为它的量测精度更高。** 两包的 14 个同名 Python 模块逐字节一致，包含两套边缘引擎、候选选择、连续性、Viterbi/ERF 和底层统计。主要差别在主流程组织、表格定义和故障隔离。

本次相同输入、相同参数的四组对照中，两版的 8 项 mixed 指标差值全部为 0。原版另外有较明显的工程问题：一个引擎失败会丢掉另一个引擎的可用结果；要求不必要的来源行和结构配对；标注失败会生成重复图片记录；PSD 失败可使主测量结果消失；会覆盖其他版本的输出。

两版仍共同存在更直接影响量测可信度的问题：默认 V13 阈值搜索漏检、单组 LWR=0 仍标 `OK`、强平滑与分组改变粗糙度带宽、未做 SEM 噪声去偏等。**换回原版不能解决这些问题。**

本次只新增审查报告、复现脚本和数值证据，没有修改两个包的算法源码，也没有把诊断实验中的临时补丁写回源码。

## 2. 检查范围与证据

审查对象：

- [原版 V1_13 ZIP](sem_cd_measure_200k_batch_V1_13_complete_package.zip)，入口 `sem_cd_measure_200k_batch_V1_13.py`。
- [V13_modified ZIP](sem_cd_measure_200k_batch_V13_modified_complete_package.zip)，入口 `sem_cd_measure_200k_batch_V13_modified.py`。
- [本次复现脚本](audits/reproduce_v1_13_comparison.py)。
- [本次完整证据](audits/v1_13_comparison_evidence.json)：源码和输入 SHA-256、依赖版本、CLI 参数、自检输出、各测试结果、导出列名和工作表。
- [此前 V13 专业量测审查](V13_CODE_REVIEW.md)，及其[独立数值实验](audits/v13_review_evidence.json)。本报告补充原版问题和版本差异，不替代此前已更正的结论。

代码定位简称如下。行号指原始 ZIP 解压后的文件；解压源码未重复提交到 Git，需从上面的 ZIP 查看。

| 简称 | 文件 |
|---|---|
| O | 原版 `sem_cd_measure_200k_batch_V1_13.py` |
| M | 修改版 `sem_cd_measure_200k_batch_V13_modified.py` |
| E | 两包相同的 `cdsem_v13_edge_engine.py` |
| S | 两包相同的 `cdsem_batch_v7_full_pipeline.py` |
| P-old / P-new | `cdsem_v112_psd.py` / `cdsem_v13_modified_psd.py` |

测试方法与边界：

1. 两个包在独立 Python 进程运行，避免同名模块导入缓存串包；运行后再次校验源码 SHA-256 未变化。
2. 原版自检通过：77 个 CLI 参数、16 种非法参数组合等；修改版的坐标统计自检和部分结果自检均通过。自检通过不等于不存在本报告中的缺陷。
3. 两包各运行四组真实示例流程，默认 trench/line 流程包含 PSD、标注和 Excel 导出；为了隔离测量，关闭自动机台比较和统计汇总绘图。
4. 每包另做 11 组故障/边界注入实验。注入使用真实检测得到的边缘，只改变失败条件、来源记录、结构标签或稳定性标记。这些是可控的软件回归测试，不代表真实 SEM 数据中这些情况的发生概率。
5. 原版另外做一次 via 冒烟测试。没有完整审查 via 算法，没有真实 SEM 标定样品、重复采集数据或商业软件对照；不能据此给出绝对准确度。

数值测试统一设 `--pixel-size 1 --trench-reference-nm 60`，line 对应使用 `--line-reference-nm 60`，`--max-number 4`。单位 nm 是测试标定下的换算，不代表可溯源的样品尺寸。

## 3. 两版到底有什么区别

| 项目 | 原版 V1_13 | V13_modified | 判断 |
|---|---|---|---|
| 两套边缘检测、连续性、Viterbi、ERF | 已有 | 使用相同模块 | 不是修改版新发明的算法 |
| CD/LER/LWR 的 mixed 来源 | CD/LWR 用 V13，左右 LER 用 V10 | 相同 | 同一项兼容性设计 |
| 旋转/未旋转坐标 | 已有两套 | 仍有两套，旋转排在前面 | 不能说原版不处理倾斜 |
| 主表结果组织 | 8 项显式 mixed 指标，加旧别名和来源审计字段 | 2 坐标 × 3 组（mixed/V13/V10）× 4 指标，24 项固定结果列 | 修改版更便于检查每个引擎 |
| 整图失败与部分结果 | 依赖两引擎和来源表共同成功 | 可独立保留有限指标，缺项标 REVIEW | 修改版容错明显改善 |
| 结构配对 | 按 role 配对，仅保留匹配项；一个也匹配不上则整图失败 | 保留匹配及未匹配项；歧义项不强配 | 修改版减少结果丢失，但未匹配不等于真实新增结构 |
| 逐点数据 | V10/V13 前缀的横向宽表；基线 512 行 | 带 engine 的纵向长表；基线 1,024 行 | 行数翻倍不是采样密度翻倍 |
| 坐标结果表 | 每张图 2 行 mixed；CSV 名为 `coordinate_system_results.csv` | 每张图 6 行，覆盖两坐标及三组；名为 `coordinate_results.csv` | 下游读取程序需要迁移 |
| 诊断资料 | 来源统计、原始跟踪、候选 debug 等较丰富 | 主输出更精简，增加 `engine_objects` | 修改版不是原版所有审计输出的完整超集 |
| PSD | 已有 | 改名、标签和展示顺序调整 | 7 个顶层数值/分组函数 AST 完全一致；并未增加噪声去偏 |
| 图案类型 | trench / line / via | trench / line | 修改版去掉 via 入口 |
| 输出目录保护 | 无版本/schema 冲突保护 | 拒绝不兼容的非空输出目录 | 同版本同 schema 的重跑仍不是不可覆盖归档 |
| CLI | 77 项，不含 help | 76 项，不含 help | 少了 `via_reference_nm`；共有参数默认值相同 |

PSD 的 AST 检查只覆盖 `runs`、`fill_short_gaps`、`spectrum`、`scalar_metrics`、`average_curves`、`strict_group`、`strict_group4`，不表示两个 PSD 文件所有代码相同。CLI 还存在 pattern 允许值和 psd-axis 选项排列的变化，详见证据。

### 3.1 旧字段不能按列位置直接替换

原版 O:510–553 明确保留旧字段作为**未旋转**结果的别名：

| 原版字段 | 修改版对应语义 |
|---|---|
| `ACD_x_nm` / `unrotated_CD_nm` | `未旋转_mixed_CD_nm` |
| `LER_left_nm` / `unrotated_LER_left_nm` | `未旋转_mixed_LER_left_nm` |
| `LER_right_nm` / `unrotated_LER_right_nm` | `未旋转_mixed_LER_right_nm` |
| `LWR_x_nm` / `unrotated_LWR_nm` | `未旋转_mixed_LWR_nm` |
| `rotated_CD_nm` 等四项 | `旋转_mixed_CD_nm` 等四项 |

修改版第一组 A–D 是旋转 mixed。若直接用旧 Excel 列号读取修改版，或把旧 `LER_left_nm` 与修改版第一组 LER 当作同一坐标结果比较，会造成口径错误。

### 3.2 原版 via 是另外一条历史流程

O:1923 将 via 交给 `sem_cd_measure_200k_batch_V1_7.py` 的 `process_all()`，不是双边缘 V10/V13 的线条粗糙度流程。示例实测返回 9 个孔，平均直径约 61.999672 nm，工作簿名为 `CD_measurement_200K_results.xlsx`。这里只验证能运行，不能把该均值与设定的 60 nm 之差直接当作算法误差，因为参考输入不等于独立几何真值。

如果后续也需要孔的 CD，可以保留并单独验证这条路径；它不能代替 line/trench 的左右 LER、LWR。

原版 `README_FIRST.md:20` 还说明历史入口共用已升级的引擎。因此，同包内的 `V1_10.py`、`V1_12.py` 不能直接当作未修改历史发布版的独立基准。

## 4. 正常输入下的数值对照

两包对应的 trench PNG 字节一致，line PNG 也一致。line 与 trench 是同一几何的反相对照，不能算两个独立样本集。

| 对照场景 | 额外参数 | 两版状态 | 比较的 mixed 指标数 | 最大绝对差 |
|---|---|---|---:|---:|
| 默认 trench | 无 | REVIEW | 8 | 0 nm |
| 默认 line | 无 | REVIEW | 8 | 0 nm |
| 仅剩一个宽度分组 | `--group-size 128 --edge-continuity 20` | OK | 8 | 0 nm |
| line 路径/ERF 变体 | `--viterbi 1 --erf-fit 1 --edge-continuity 50 --group-size 8` | OK | 8 | 0 nm |

默认 trench 的旋转 mixed 结果，两版均为：

| 指标 | 数值（nm） | 来源 |
|---|---:|---|
| CD | 59.8193558903 | V13 |
| 左 LER | 2.2949095155 | V10 |
| 右 LER | 2.3071893010 | V10 |
| LWR | 0.7311518975 | V13，group4 |

这证明这四组受测条件的兼容性，不证明所有输入、缺失情况或参数组合都等价。修改版故障降级行为本来就不同。路径/ERF 变体同时改变多个选项，也不能用它的结果单独证明 ERF 更准确。

## 5. 原版特有或更严重的问题

优先级：P1 为可能丢失结果、误报成功/失败或覆盖数据的问题；P2 为可追溯性、接口稳定性问题。下面的“有限指标数”均指两坐标共 8 项 mixed，不是修改版全部 24 项。

### O1 — P1：一个引擎失败，另一个引擎的可用指标也被丢弃

定位：O:1335 附近的两次 `measure_image()`，及 O:1546 附近的整图异常捕获；对照 M:285、M:333。

两引擎处于同一个大 try 中。V10 先失败时甚至不会运行 V13；V13 失败时，已经计算出的 V10 也不会进入主结果。

复现：分别注入 V10/V13 检测异常，原版均只有一个 ERROR 行，8 项 mixed 全部缺失。修改版均保留 4 项：V10 失败仍保留 V13 的两坐标 CD/LWR；V13 失败仍保留 V10 的两坐标左右 LER，并标 REVIEW。

建议：按引擎、指标隔离失败，保留成功结果，同时明确缺失原因；不能为了“结果完整”用另一引擎悄悄补齐来源。

### O2 — P1：不必要的来源行依赖会使主结果失败

定位：O:592–608 的 `hybrid_primary_image()`；O:1389 附近的来源检查。

原版要求 V10 raw、V10 group、V13 raw、V13 group 四行全部存在。mixed 的 LER 使用 V10 raw，V10 group 只是审计信息，却也成了整图硬门槛。

复现：仅移除 V10 的 group 来源行，保留真实边缘和其余来源，原版整图 ERROR；修改版仍有全部 8 项 mixed。让 V10 无候选时，原版也丢掉本来可用的 V13 CD/LWR，修改版保留这 4 项。

建议：来源审计缺项与目标指标不可计算分开处理；主统计直接依赖实际需要的边缘/指标，非必要审计信息只能产生诊断警告。

### O3 — P1：结构 role 对齐被错误地作为整图可用性的前提

定位：O:692–718 的 `hybrid_primary_objects()`；O:1398 附近的 `if not objects`；对照 M:368。

两个引擎的整图均值原本独立计算，但原版还要求至少有一组对象 role 能配上。测试只给两边 role 加上不同前缀，保留全部坐标和数值，原版整图 ERROR；修改版保留 8 项 mixed，并导出 8 条未配对的单引擎对象记录。

这 8 条记录不代表图里有 8 条物理线。修改版避免了数据丢失，但仍需要按几何位置/重叠关系验证对象配对，不能把相同 role 当作充分的物理身份依据，也不能把未配对项直接当成独立样本合并。

建议：整图指标与跨引擎结构关联分层处理；对象表保留各自 ID、配对状态和配对置信度。

### O4 — P1：标注失败会让同一张图片出现两条相互冲突的状态记录

定位：O:1434 先加入主结果，O:1505 保存标注，O:1560 异常后再次加入 ERROR 行；对照 M:1161–1183。

注入标注写入异常后，原版处理 1 张 PNG，却输出 2 行：一个 REVIEW 且含全部指标，另一个 ERROR 且无指标。settings 同时记 `processed_png_count=1`、`review_count=1`、`error_count=1`。

因此这个问题不是“标注失败会删除全部主结果”，而是**重复行、互相矛盾的状态和失真的成功/失败计数**。修改版在本场景只保留一个 REVIEW 行，并把 `V13_annotation` 写入诊断错误表。

建议：每图只提交一次主结果；测量质量、标注导出状态分别记录。加入 `image_key` 唯一性和状态计数守恒测试。

### O5 — P1：PSD 的附属处理异常可以阻断主 CD/LER/LWR

定位：O:1339–1340 在主指标保存前调用 `PSDBatch.add_measurement()`；对照 M:1169 附近的分阶段捕获。

仅注入 V13 PSD 加入批处理时异常，原版主表为 ERROR、8 项 mixed 全缺失；修改版主表仍保留全部 8 项并标 REVIEW，另记 `V13_PSD` 错误。

建议：PSD 和标注同样是可独立失败的产物，不能拖垮已经完成的边缘量测。这里验证的是 `add_measurement()` 阶段，不表示修改版最终 PSD 工作簿写入也已经全面隔离。

### O6 — P1：机台参考表匹配失败会中止导出；修改版只部分缓解

定位：O:1151、O:1188，及 O:1620–1635 的机台比较调用；对照 M:1218–1235、M:822。

输入一份合法的 v2 工作表、但文件名全部不匹配：

- 原版退出码 1，主 `image_summary.csv` 和主 Excel 都没有生成。机台比较发生在主 CSV 写出前。
- 修改版也退出码 1；主 CSV 已保留 8 项 mixed，但主 Excel 和本次 settings 仍未生成。

所以不能说修改版已经修好所有附属步骤异常。根目录存在 `Data.xlsx` 时默认还会自动启用比较，使这个问题容易在批量运行后才暴露。

建议：先完成主结果和 settings 的可靠导出；机台对照使用独立失败状态，未匹配只形成未匹配表和警告。需要机台对照作为验收门槛时，可以最终返回非零状态，但不应丢失主测量产物。

### O7 — P1：没有输出版本冲突保护，可能覆盖已有结果

定位：O:1279–1280 的目录初始化及 O:1665 附近的 CSV 写入；对照 M:1099–1111。

在临时输出目录预放一份不同版本的 settings 和哨兵 `image_summary.csv`：原版继续执行并覆盖原表；修改版拒绝执行，保留原表。

测试仅在审查脚本创建的临时目录中进行，没有覆盖用户数据。建议每次运行使用独立 run ID 目录，并将写入完成状态、参数和源码摘要一起归档。修改版的版本检查不等于同版本重跑的防覆盖保护。

### O8 — P2：对象“参与统计”标记写死，影响可追溯性

定位：O:754 的 `used_for_statistics=True`；实际选择逻辑在 S:859；对照 M:927–944。

原版给全部配对对象写 True，却没有核对对象是否真的进入图像均值。构造 4 条已有测量对象、仅后 3 条 stable 的情况，实际选择集合排除第 1 条，但原版对象表仍全部标 True。修改版分别输出 `V10_used_for_image_statistics`、`V13_used_for_image_statistics`，能标出各引擎未入选的对象。

建议：按引擎从实际聚合集合生成标记；将“被检测”“有数值”“通过质量检查”“参与主汇总”分成不同字段。

### O9 — P2：全失败批次的主表缺少结果列

定位：O:1560 的错误行结构与 O:1575 附近的 DataFrame 构造；对照 M:895、M:1197。

两引擎均无候选时，原版 `image_summary.csv` 只有元数据、状态和错误信息，没有 CD/LER/LWR 列。修改版同样正确标 ERROR，但仍保留全部 24 个结果列，值为空。

原版的下游脚本不能只根据固定列名读取，可能在最需要汇总失败情况时抛出 KeyError。建议无论成功与否都使用固定 schema，并将缺测值与零值严格区分。

## 6. 两版共有的量测问题，不应误归因于版本差异

| 问题 | 本次/此前证据 | 对你的影响 |
|---|---|---|
| V13 阈值定义和仅向内的交点搜索不匹配 | E:450、E:624；两版默认 V13 都只有 175/512 有效点，V10 为 512/512 | CD/LWR 可能由非随机留下的少量位置决定 |
| 单分组仍输出零粗糙度 | S:351；两版 `group-size=128, continuity=20` 都返回两坐标 mixed LWR=0，状态 OK | “支持不足”可能被当作“没有粗糙度” |
| 沿线 32 行平均、128 槽位、默认 group4 | 同一引擎与统计；此前有已知正弦和交替宽度实验 | 平滑压低高频粗糙度，不同配方结果不可直接混比 |
| mixed 混用引擎，LER 不分组、LWR 分组 | 两版来源相同 | 不能解释为同一组边缘、同一带宽下的自洽统计 |
| PSD 没有 SEM 测量噪声去偏 | P-old/P-new 的频谱与标量函数相同 | “输出 PSD”不等于“无偏 LER/LWR” |
| REVIEW 仍进入条件汇总和机台对照 | O:1610；M:1208 | 低支持、补点或部分失败结果可能影响汇总均值 |
| 主统计和 PSD 有效支持/滤波策略不同 | 默认两版均为 V10 的 40 条谱 OK、V13 的 40 条谱全部 SKIPPED | 主表有 LWR，并不代表该 LWR 已得到有效 PSD 佐证 |
| 补点、强平滑、旋转几何、标定和不确定度问题 | 详见此前审查 F3–F10 等章节 | 需要独立验证，不能靠增加输出列解决 |

默认 V13 的无效原因为 236 次 `left_threshold_crossing_not_found` 和 101 次 `right_threshold_crossing_not_found`。40 条谱是 4 条结构 × 2 坐标 × 5 类信号，不是 40 张图或 40 个独立样本。

特别保留此前的更正：**不能把主流程说成“左右 NaN 独立分组导致错配”**。原版 O:339 与修改版 M:438 的统计函数都建立共同有效掩码。真实问题是各组支持数不一、组数不足和分组造成的带宽变化。

这些问题中有实现缺陷，也有量测定义或验证能力缺口；不是每一项都能靠改一个公式修好。`ddof=0` 描述有限序列的总体标准差，本身不等于程序写错；简单改为 1 也不能消除空间相关、平滑和 SEM 噪声偏差。

## 7. 与专业量测要求相比，应优先改什么

### 第一阶段：先让结果不丢失、不误报

以修改版主流程为基础，修复两版共有的阈值交点搜索和单组零 LWR；把机台比较、PSD 最终导出、标注与测量结果保存进一步分离。每图保持一个主记录，给每项指标保存有效点数、有效组数、有效长度、人工补点比例和失败原因。

保留原版较丰富的候选和阈值诊断资料，但作为可选诊断输出恢复，不应重新成为主结果成功的前提。自动化回归应包括本报告的 11 个边界场景，而不只检查“正常输入能够出 Excel”。

### 第二阶段：明确 CD、LER、LWR 的量测口径

建议同时报告单引擎结果及明确标注的兼容 mixed；对主粗糙度指标统一边缘、有效位置、去趋势和空间频带，并保留 `LWR_raw_3sigma` 与 `LWR_groupN_3sigma` 的区别。CD 可以使用与粗糙度不同的定位/平均配方，但必须分别命名、验证，不把较平滑的数值自动解释成较准确。

测量长度和采样间隔本来就决定纳入哪些粗糙度空间频率；跨版本、跨配方或与机台对照，首先要对齐这些条件。[NIST：Issues in Line Edge and Linewidth Roughness Metrology](https://tsapps.nist.gov/publication/get_pdf.cfm?pub_id=822535)

### 第三阶段：建立噪声去偏与不确定度证据

现有两版都应称为指定图像/配方下的 measured LER/LWR，而非已经去偏的样品固有粗糙度。SEM 图像噪声可给 LWR 引入系统性正偏差，这是独立于表格是否漂亮、程序是否运行成功的计量问题。[NIST：Unbiased Estimation of Linewidth Roughness](https://www.nist.gov/publications/unbiased-estimation-linewidth-roughness)

建议使用已知几何的合成数据、独立真实 SEM 验证集和重复采集，分别评估阈值、倾角、pitch、模糊、噪声、像素标定和处理配方的影响。噪声去偏需要验证噪声模型以及左右定位误差的相关性；不能对已经强平滑的 PSD 随意减一个常数，也不能把同一张图的 128 个高度重叠槽位当作 128 次独立重复。

这里是基于代码和原始文献的改进方向，不是与某商业软件实测后得出的性能排名。

## 8. 如何复现

先将两个 ZIP 分别解压到仓库内同名目录，并按包的 requirements 配置 Python 环境。本次使用已有 `.venv`；Python 为 3.12.3，具体库版本保存在证据 JSON。

在仓库根目录运行：

```bash
audit_tmp=$(mktemp -d /tmp/measure-v1-13-comparison-XXXXXX)
.venv/bin/python audits/reproduce_v1_13_comparison.py --package original --output "$audit_tmp/original.json"
.venv/bin/python audits/reproduce_v1_13_comparison.py --package modified --output "$audit_tmp/modified.json"
.venv/bin/python audits/reproduce_v1_13_comparison.py --combine "$audit_tmp/original.json" "$audit_tmp/modified.json" --output audits/v1_13_comparison_evidence.json
```

脚本内部使用临时目录生成测试结果，收集证据后自动清理；上述两个中间 JSON 留在 `audit_tmp`，便于排查。重新运行会更新仓库内的证据 JSON；临时路径和日志路径可能变化，不要求整份 JSON 字节一致。

证据中的 `source_files_unchanged=true`、`fixture_hashes`、`compared_mixed_metric_count` 和 `max_abs_mixed_metric_difference_nm` 可用于确认比较对象和数值覆盖范围。注入测试没有改变原始 ZIP、PNG 或量测源码。

## 9. 使用建议

如果只测线条/沟槽的 CD、LER、LWR，继续维护修改版更合适；需要孔 CD 时，保留原版 via 为单独验证的工具。原版的丰富诊断输出值得吸收，但没有证据说明退回原版会提高准确度。

在解决阈值漏检、最低统计支持、统一带宽、噪声去偏与标定验证之前，两版都更适合探索性图像分析和配方内相对比较，不应直接作为已验证的专业绝对量测系统。
