# V1_16 修改记录

## 2026-09-21：独立发布 V1_16

基于原始 V1_15（`41e3294`），保留提交 `ae11296` 已实现的改进。本版单独建目录、入口、依赖清单、启动器、输出文件名、测试报告和 ZIP；原 V1_15 全部文件与 ZIP 恢复到 `41e3294`。本次通过新提交整理版本，保留原提交历史。

- 修复固定 145nm 候选上限，按参考宽度扩展，双极性共用。
- 修复内部暗条导致 line 反相后分裂、自动估宽及 ROI 错误；补充近边缘平台阈值参考，最终仍测量原图真实边缘。
- 自动测旋转 pitch CD：相邻一个 line+trench；按 max-number 个完整周期等权平均，额外相邻结构仅用于 pitch。
- Excel 首表最后一列 `旋转_pitch_CD_nm`；mixed/V13 用 V13，V10 用 V10。周期不足为 REVIEW，缺失为空值；输出逐周期和逐点审计。
- 新增 line/pitch 回归并保留原有数值、PSD、Viterbi/ERF、背景和异常隔离测试。
- V1_16 单独输出 `CD_measurement_200K_V1_16_results.xlsx`；仍识别并跳过 V1_15 历史结果目录。

完整自检覆盖 36 个独立端到端/故障场景及解析测试；另验证版本标识、默认输出路径和多版本结果目录排除。详见 `validation_report.json`、`RUN_GUIDE.md` 第 12 节；仓库的 `audits/v1_16_release_evidence.json` 保存原 V1_15 字节一致性、迁移算法一致性及 ZIP 解压验证。

内部 `v115` / `V1.15` 函数名是继承的历史定位算法标识；当前发布版本由 SCRIPT_VERSION=`V1_16` 标识，不依赖旧目录。

验证使用合成图，尚无用户实测失败 SEM 图片。数量不足不重复计数、不伪造结果，灰度无法区分的结构仍须核查。
