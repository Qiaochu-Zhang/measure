# V13 的 CD、LER、LWR 代码复核与专业量测方法对比

审查日期：2026-09-18。审查基线：仓库提交 `53e972c` 中的两个原始 ZIP，主要入口为 `V13_modified`。本报告替代上一版 `V13_CODE_REVIEW.md`。

**结论：程序可以运行，但默认配置下存在已复现的边缘漏检问题，现有 LER/LWR 也不能直接解释为已经去除 SEM 噪声、具有统一空间带宽的样品粗糙度。** CD 可作为指定阈值、标定和配方下的图像测量值；要用于工艺评价或与机台比较，需要先修复漏检和统计支持不足，再明确带宽、噪声去偏及质量门槛。

本次完成了源码检查、两项原有自检、包含 PSD 和标注图导出的示例运行，以及独立统计/滤波/阈值搜索实验。没有真实 SEM 标定数据，也没有运行商业软件作数值基准比较；下文的专业对比依据公开原始论文和项目资料，不声称已达到某设备的精度。本次仅更新审查报告和复现材料，两个包内的算法源码保持原样。

## 1. 对上一版报告的更正

1. **撤回“主流程分组时左右缺点错配”的结论。** 主程序第 465–473 行已经建立共同有效掩码，并同时把左右边缘的无效点设为 NaN。在此条件下，同组的左右均值之差等于共同有效局部宽度的均值。本次专门构造左右不同位置缺失的数据，计算结果与共同掩码参考值均为 LWR = 1.5 nm。真正存在的问题是：不同组的有效点数不一致、只有一个有效组仍输出零，以及分组平均改变带宽。
2. **mixed 并非隐蔽的程序错误。** README 明确说明 CD/LWR 来自 V13、LER 来自 V10。这是已声明的兼容性设计，但不适合被解释为同一组边缘的自洽统计。即使使用同一引擎，未分组 LER 与分组 LWR 的带宽也不同。
3. **line/trench 示例数值相同是预期的极性验证结果。** 已核实两个 512×512、uint8 PNG 满足 `line = 255 - trench`，最大像素差为 0。line 模式又反相一次，因此得到相同指标正常；它们只是同一几何的极性对照，不能算两个独立的真实 SEM 验证集。
4. **不应把“所有质量不足结果均来自新 fallback”写成根因。** 旧版 `choose_aggregate_space_indices()` 已在稳定结构不足时选取有数值的不稳定结构；修改版还有额外 fallback。默认示例首先走的是旧版降级选择。
5. **“200K 像素尺寸错误会同比缩放结果”需加条件。** 对固定边缘坐标，nm 指标按比例变化；但像素尺寸还参与参考 CD 的像素换算和检测窗口约束，重新运行整个检测流程时，变化可能不只是线性缩放。

## 2. 审查范围、入口与证据

两个原始包：

- [V13_modified 完整包](sem_cd_measure_200k_batch_V13_modified_complete_package.zip)
- [原 V1_13 完整包](sem_cd_measure_200k_batch_V1_13_complete_package.zip)

两个包的 `cdsem_v13_edge_engine.py` SHA-256 相同：

```text
fade515779f37cb15072b66da08c9fabed01aa2f39fc0998d4048d285891e973
```

因此，边缘引擎中的漏检问题也涉及原 V1_13 的这个引擎；修改版的主表、部分结果和质量汇总行为则按修改版入口单独审查，不能直接推广到所有旧入口。

为便于定位，下文代码简称如下。行号对应 ZIP 解压后的源码，不是本报告行号。

| 简称 | 文件 |
|---|---|
| M | `sem_cd_measure_200k_batch_V13_modified.py` |
| E | `cdsem_v13_edge_engine.py` |
| S | `cdsem_batch_v7_full_pipeline.py` |
| P | `cdsem_v13_modified_psd.py` |
| C | `cdsem_v112_continuity.py` |
| F | `cdsem_v113_path_erf.py` |

可复现材料：

- [审查复现脚本](audits/reproduce_v13_review.py)
- [完整数值证据、依赖版本及源码 SHA-256](audits/v13_review_evidence.json)

后文数值实验使用 `--pixel-size 1`；标为 nm 的值是按这个测试标定换算的结果，不代表真实样品的尺寸溯源。

## 3. 当前算法实际在做什么

处理链条为：

```text
PNG → 灰度归一化 / line 反相 → 粗定位结构
    → 沿 Y 取 128 个采样槽位
    → 每槽位平均 32 行灰度，再做横向 3 点平滑
    → 梯度峰定位 + 阈值亚像素插值 + 跟踪/剔除/恢复
    → 可选 Viterbi、ERF、连续性恢复和 flyer 处理
    → 每条结构拟合 PCA 中心线，转换坐标
    → CD/LER/LWR → 每结构算术平均 → 每图与每文件夹输出
    ↘ 独立 PSD 分段、去趋势、分组与汇总
```

对固定坐标系中的同一条结构，令左右边缘为 `xL(i)、xR(i)`：

- CD：共同有效点上 `mean(xR - xL) × pixel_size`。
- 左/右 LER：各边缘坐标的 `3 × std`，默认 `ddof=0`。
- 主表 LWR：先对连续槽位的宽度取组均值，再取组均值的 `3 × std`；默认每 4 点一组。
- 旋转结果：左右边缘共同使用中点轨迹的 PCA 方向，不是左右分别拟合去趋势。
- 未旋转 LER：包含图像整体倾斜对横向坐标波动的贡献。
- 图像级值：入选结构的指标算术平均，不等于把所有结构残差合并后的 3σ。
- mixed：V13 的 CD/LWR 加 V10 的左右 LER。

总体标准差 `ddof=0` 本身不是算错。它描述当前有限测量段；若要估计过程方差或置信区间，还需要处理拟合自由度、空间相关及有限长度效应，不能简单把 `ddof` 改成 1 就宣称无偏。

## 4. 按影响排序的发现

### F1 — P0：V13 阈值定义与交点搜索范围不匹配，造成系统性漏检

类型：已复现的算法实现问题。定位：E:450、E:624、E:829；C:26 附近的 `extend_threshold_search()` 也明确描述了这一兼容问题。

V13 用中央暗带和外侧亮带均值定义阈值，但 `interpolate_threshold_crossing()` 仍只从选中的梯度峰向结构内侧搜索。新的阈值交点可能位于峰的外侧，于是存在可用边缘时也返回“未找到交点”。默认 `--edge-continuity 0` 不执行已有的向外扩展恢复。

在随包 trench 示例中，4 条结构 × 128 点共 512 个采样槽位：

- V10：512 点全部有效。
- V13：175 点有效，整体有效率 34.18%；单条结构分别为 42、45、44、44 点。
- 无效原因：236 次 `left_threshold_crossing_not_found`，101 次 `right_threshold_crossing_not_found`。
- V13 稳定结构为 0，图片最终为 `REVIEW`。

只在审查进程内向外扩展搜索起点 2 px，保持阈值、拓扑、宽度范围和跟踪门槛不变，得到：

| V13 实验 | 有效点 | 稳定结构 | 人工补点 | 旋转 CD | 旋转左 LER | 旋转右 LER | 旋转 group4 LWR |
|---|---:|---:|---:|---:|---:|---:|---:|
| 原默认流程 | 175/512 | 0 | 0 | 59.819356 | 2.053733 | 2.105619 | 0.731152 |
| 仅向外扩展搜索 2 px | 512/512 | 4 | 0 | 59.862719 | 2.112120 | 2.129792 | 0.582574 |
| 原代码开启 continuity=20 | 512/512 | 4 | 0 | 59.862724 | 2.112129 | 2.129792 | 0.582604 |

单位为测试标定下的 nm。搜索扩展实验的 LWR 比原默认结果低约 20.3%，说明缺点选择会实质影响粗糙度，并非只有状态标签变化。

这支持修复交点搜索，但**不能证明“固定加 2 px”适用于所有真实 SEM 图**。建议在原锚点窗口中枚举符合梯度方向的阈值交点，保留几何和拓扑限制，按锚点距离及信号证据排序，再用不同 pitch、噪声、CD 和倾角验证。不要仅靠降低 `minimum-valid-fraction` 消除警告；continuity 参数还同时放宽其他条件，也不宜直接当通用修复。

### F2 — P1：统计支持不足时输出 LER/LWR = 0，容易把“无法估计”当成“零粗糙度”

类型：已复现的统计边界缺陷。定位：S:351 `three_sigma()`、M:513–554、`cdsem_v113_parameters.py` 的 group-size 校验。

`three_sigma()` 只要求有限点数大于 `ddof`；默认 `ddof=0`，因此一个点或一个分组就能返回 0。

独立调用主统计函数时，仅一个有效宽度 60 nm，就得到 CD=60、左右 LER=0、LWR=0。这与说明书“支持不足时留空、不补成 0”的表述不符。这里的零是单点总体方差公式产生的退化值，不足以支持“样品无粗糙度”的推断。

参数校验还接受 `--group-size 128 --sample-number 128`，于是整条结构只剩一个组，无论沿线宽度怎样变化，LWR 都为零。本次用 `--edge-continuity 20 --group-size 128` 对随包图做完整运行，结果是六组旋转/未旋转 LWR 全部为 0，图片状态仍为 `OK`。这说明图像级质量门槛没有检查 LWR 实际只剩一个有效组。

建议每个指标分别记录有效点数/组数、有效长度及最低支持条件。CD 可以在少量点下保留为诊断均值；LER/LWR 在只有一个有效点/组时必须为缺失值。至少两个是数学计算门槛，可信量测通常需要更充分的空间支持，具体门槛应由验证确定。

### F3 — P1：默认空间平均与 group4 改变粗糙度带宽，可消除真实波动

类型：已声明但容易误读的量测定义与滤波风险。定位：E:1676、E:1682；M:530–554；S:710。

默认沿线测量长度参数 360 px，128 个槽位的首尾实际跨度为 359 px：

- 名义采样间隔：359/127 ≈ 2.8268 px。
- 平均窗口：32 行。
- 相邻窗口约重叠 91%，128 个采样点不能视为 128 个独立观察。
- 默认 1.3181 nm/px 时，首尾跨度约 473.20 nm，采样间隔约 3.726 nm，平均窗口宽约 42.18 nm。
- group4 还会进一步平均和降采样；这不是噪声去偏。

两项独立实验：

1. **已知无噪声正弦边缘，隔离行平均的影响。** 真值为 `3σ = 1.06066 px`。在相同阈值提取下：

   | 边缘波长 | 平均 1 行的测量 LER | 平均 32 行的测量 LER |
   |---|---:|---:|
   | 8 px | 1.06482 px | 0 px |
   | 32 px | 1.06515 px | 0 px |
   | 128 px | 1.06515 px | 0.96135 px |

   此实验直接调用行平均与交点函数，未运行粗定位、跟踪和完整 SEM 检测，目的是证明滤波的带宽效应，不能当作完整流程误差预算。

2. **已知宽度序列交替为 59/61 nm，共 128 点。** 未分组 LWR=3 nm；group4 后 LWR=0。代码完成了定义要求的平均，但结果已是另一种粗糙度指标。

建议同时输出未分组 `LWR_raw_3sigma`、带明确空间尺度的 `LWR_groupN_3sigma` 和固定频带 PSD 积分值。CD 定位可以使用较强平均；粗糙度最终提取应使用经验证的更细采样和更小沿线窗口，并报告其传递特性。

专业依据：有限长度和采样间隔本来就限定了粗糙度包含的空间频率，跨配方比较必须统一这些范围。[NIST：Issues in Line Edge and Linewidth Roughness Metrology](https://tsapps.nist.gov/publication/get_pdf.cfm?pub_id=822535)

### F4 — P1：没有 SEM 噪声去偏，PSD 输出不等于 unbiased LER/LWR

类型：专业量测能力缺口。定位：P:31、P:81；主表统计见 M:513–570。

当前 PSD 实现计算 Welch/periodogram 和频谱积分，没有估计边缘定位噪声底、扣除测量噪声或输出其不确定度。实际结果混合了样品波动、图像噪声、滤波和检测选择效应。

当真实波动与测量噪声独立时，最基本的关系是：

```text
Var(measured) = Var(sample) + Var(measurement_noise)
```

宽度的噪声还取决于左右定位误差的相关性：

```text
Var(noise_R - noise_L)
= Var(noise_R) + Var(noise_L) - 2 Cov(noise_L, noise_R)
```

所以不能简单认为“平滑后更小的 LWR 就更接近真值”，也不能对左右噪声无条件假定独立。NIST 的无偏 LWR 工作指出，图像噪声会对粗糙度造成系统性正偏差。[NIST：Unbiased Estimation of Linewidth Roughness](https://www.nist.gov/publications/unbiased-estimation-linewidth-roughness)

建议先保留未经沿线强平均的边缘轨迹，再评估高频段是否确有可识别的噪声底；可用重复图像/帧差等独立证据校验。若噪声为有色噪声，必须使用相应模型。V13 当前经过 32 行平均、重叠采样和 group4 后的高频谱，不适合直接套“平坦白噪声底相减”。

物理线扫拟合加 PSD 噪声估计是一条专业路线，但需要识别条件和验证，不能仅添加一个减常数函数就命名为无偏结果。[Lorusso 等：Unbiased Roughness Measurements—Subtracting out SEM Effects, Part 3](https://www.lithoguru.com/scientist/litho_papers/2019_SEM%20effects%20part%203.pdf)

### F5 — P1：REVIEW 数据自动参与条件汇总和机台比较

类型：已复现的质量传播风险，而非“完全没有质量标记”。定位：S:859；E:2379；M:1208、M:981、M:1230 附近。

当前代码有 `OK/REVIEW/ERROR` 和有效率字段，这是优点。但后续使用：

```python
valid_images = image_df[image_df["status"] != "ERROR"]
```

因此 REVIEW 会进入文件夹平均、统计图和机台比较。最小验证：同文件夹一张 OK 的值为 10、一张 REVIEW 的值为 100，条件均值是 55。条件表虽然保留了 OK/REVIEW 数量，均值本身仍混合了两者。

建议保留 REVIEW 诊断数据，但增加明确的 `qc_pass` / `metric_eligible`；正式均值、比较和图表默认只使用符合该指标质量门槛的数据，同时输出另一个“含 REVIEW”的对照。最低有效率不能只用于改变文字标签。

另外，`V13_selection_fallback` 只描述新入口的额外 fallback，并不覆盖 S:859 内部已经选入不稳定结构的情形。建议统一记录 `selection_policy`、入选稳定/不稳定数量和逐指标入选集合。

### F6 — P1/P2：mixed 与“原始 LER + 分组 LWR”不构成统一统计

类型：定义和结果解释风险。定位：M:884、M:576。

同一边缘集合、相同坐标、相同滤波及相同去趋势下，存在代数关系：

```text
LWR² = LER_L² + LER_R² - 2 ρ LER_L LER_R
```

这里各指标都按同一个 3σ 约定计算。当前 mixed 使用不同引擎，且两引擎可独立选择结构；同引擎主表也用未分组 LER 配分组 LWR，所以不能直接用主表四个数字推断边缘相关性。

示例旋转 CD：V13=59.8194，V10=57.6449，相差约 2.1745 nm。**没有物理 CD 真值，不能仅据此认定 V13 更准确或 V10 更差。**

建议把“同引擎、同支持、同频带”设为科学统计输出，保留 mixed 作为兼容性对照。增加未分组 LWR、左右相关系数、中心线粗糙度及同频带互谱。跨结构汇总需说明是平均每条 3σ，还是先合并/平均方差再开方；二者都可定义，但不能混称同一个估计量。

### F7 — P2：分组有效点数不一致；PSD 与主表使用不同支持集

类型：可运行但需要清楚限定的统计策略。定位：S:710；P:116、P:190、P:250。

共同掩码已经保证左右配对正确。但主 LWR 中一个组可以只有 1 个有效点，另一个有 4 个，它们的组均值在标准差里权重相同；尾部不足 4 个点的组也参与。不同缺失率相当于使用不同的平均强度。

PSD 则要求每组所有点有效，并默认分连续有效段计算。还存在以下区别：

- 主表按选中的结构汇总；PSD 在 `finalize()` 中按已有曲线汇总，`measurement_stable` 是记录字段，并未作为同样的入选门槛。
- PSD 默认线性去趋势，主表是共同中心线旋转后的坐标波动。
- 默认示例中 V13 的 4 结构 × 2 坐标 × 5 信号 = 40 项 PSD 全部 `SKIPPED`，V10 对应 40 项均可计算；两个极性示例都如此。
- 单图“有主表值”并不代表有可用的 PSD 噪声分析。

建议让主统计和 PSD 共享一个显式的逐指标质量/支持选择模块，报告每组有效数、实际空间位置、连续长度和 PSD 使用窗口数。允许二者采用不同定义时，必须在列名和元数据中体现，不能用积分不相等直接判定其中一方公式出错。

### F8 — P2：人工补点和过强路径约束可能改变待测粗糙度

类型：可选功能风险，默认 continuity=0 时没有强制补点。定位：C:155 附近、M:465、M:1144；F 的 `viterbi_path()`、`fit_erf()`、`apply_final_flyers()`。

continuity=100 时，剩余缺点可能线性插值、端点保持或使用粗定位锚点补齐，并标为 `continuity_synthetic=True`。主表只检查 `valid`，仍把这些点纳入统计；PSD 默认排除它们。图片会被标 REVIEW，但粗糙度数值已经受补点影响。

函数级验证：两个实测宽度 59、61 nm 的 LWR=3 nm，再补入六个 60 nm 的合成有效点，主统计得到 1.5 nm。这个例子说明统计纳入规则的影响，不是对默认完整流程结果的声明。

Viterbi 的跳动惩罚、flyer 移除/替代和 robust postfilter 也可能抑制真实高频粗糙度。它们可以改善错边跟踪，但需要用具有已知频谱和缺陷的信号量化“保留真实变化、剔除伪边”的能力。强制补点应默认仅用于显示或单列诊断估计。

### F9 — P2：旋转投影不总等于同一法向截面的局部线宽

类型：适用范围限制。定位：M:475–507；E:1840。

同原图 Y 处的两个边缘点旋转后满足：

```text
Δx′ = (xR - xL) cos θ
Δy′ = (xR - xL) sin θ
```

对平行直边，乘 cosθ 得到法向间距正确，原自检也覆盖了这一情形。对沿线变化明显的粗糙/弯曲边缘，两点的 Y′ 不同；它们的 X′ 差并不严格是同一法向截面的宽度。

若要支持明显倾斜、弯曲或强非平行结构，应在拟合方向的同一轴向位置重采样左右边缘，或沿法线与两条曲线求交，再验证插值引入的频谱变化。当前实现适合近竖直、小倾角近似，不应扩展解释为任意方向的二维轮廓计量。

另一个采样细节是 `average_rows_profile()` 将名义 y 四舍五入到整数行，32 行窗口的实际中心也不同于某些名义采样 y。更细致的 PSD 实现应保存实际采样位置或采用受控的亚像素采样，验证倾斜边缘下的影响。

### F10 — P1/P2：绝对 CD 标定与复现性信息不足

类型：量测溯源和工程能力缺口。

- M:1295 的 `--pixel-size` 默认 1.3181 nm/px，整批使用同一标定；没有读取 SEM 标尺/元数据或处理 X/Y 独立标定。
- 固定 50% 灰度交点只是操作性边缘定义，不自动等于 top CD、bottom CD 或某一侧壁高度的物理 CD。
- E:351 使用整幅图的 min/max 归一化；文字/标尺/极端亮点可能改变归一化后的固定对比度门槛。自动参考宽度估计另有百分位归一化路径，建议统一数据预处理与 ROI 定义。
- 仅处理 PNG；这是已声明范围，不能直接把各种设备原始 TIFF 和带元数据格式等同输入。
- requirements 只给最低版本；settings 记录参数，但缺少完整依赖锁定、代码/输入哈希和量测不确定度。审查证据额外保存了版本与源码哈希。
- 大批量时主程序把所有图的 sample rows 保存在内存并写入单个工作簿，应规划分块/流式输出及 Excel 容量检查；本次没有做规模压力测试。

建议实际 nm/pixel 必填或通过可信元数据获得，保留其来源和不确定度。绝对 CD 比较应使用已知样片或独立参考方法校准，而不是用拟合机器输出误差最小的阈值直接当物理真值。

## 5. 与专业方法的具体对比

“专业算法”并不是一个通用且唯一的公式。应比较边缘物理定义、滤波、噪声校正、空间带宽及不确定度，而不是仅比较输出的数值是否接近。

| 能力 | 当前 V13 | 可借鉴的专业路线 | 本项目优先改进 |
|---|---|---|---|
| 边缘定位 | 梯度峰 + 相对灰度阈值；可选 ERF | 受约束线扫模型；进一步为电子散射模型库 | 修复交点搜索，再比较阈值/ERF/物理模型的系统偏差 |
| 沿线提取 | 32 行平均与重叠采样 | 有明确带宽的细采样，分开粗定位与最终边缘估计 | 保留高分辨率边缘，记录每一步滤波 |
| 噪声处理 | 主表无去偏，PSD 只分析频谱 | 测量噪声估计及 PSD 去偏 | 先验证噪声模型和可识别频带 |
| 统计关系 | 跨引擎 mixed，LER/LWR 带宽不同 | 相同边缘、掩码、频带的 LER/LWR 与互谱 | 增加统一统计输出 |
| 形貌描述 | CD、左右 LER、分组 LWR、PSD 积分 | 频带粗糙度、相关长度、适用时的粗糙度指数 | 避免只用单一 3σ 排序样品 |
| 可信度 | 有效率、REVIEW、异常日志 | 重复性、偏差、不确定度、参考样品验证 | 按指标传播质量与置信信息 |

### 5.1 CD：ERF 可作过渡，物理模型拟合才进一步关联样品几何

现有 `fit_erf()` 已有受约束优化、鲁棒损失、最大位移和相对残差门槛，值得保留。但它用对称单调灰度过渡模型，且拟合的是已平均和平滑的剖面。真实 SEM 的边缘增强、材料差异、非对称响应和充电未必满足这个模型，开启 `--erf-fit 1` 不保证精度提高；拟合成功也不等于几何位置正确。

建议先保存完整拟合残差、拟合成功比例和位置不确定度，用相同 ROI 对比阈值与 ERF。只有在明确材料/电压/结构范围且具备参考数据时，再引入物理线扫响应或 Monte Carlo 模型库。

NIST 的 MBL 方法通过电子输运仿真建立响应库，再拟合几何参数；它提供了把 SEM 灰度与样品形貌联系起来的路线，而不是认定任意固定阈值都代表同一物理边缘。[NIST：Scanning Electron Microscope Dimensional Metrology Using a Model-Based Library](https://www.nist.gov/publications/scanning-electron-microscope-dimensional-metrology-using-model-based-library)

### 5.2 LER/LWR：保留原始轨迹，明确频带，估计噪声

建议增加一条可审计的粗糙度分析流程：

1. 使用粗定位确定结构与搜索窗口。
2. 在细采样剖面上估计亚像素边缘；保存位置、拟合质量、真实/恢复/合成点标记。
3. 明确去趋势方式：共同中心线、各边缘独立直线、或规定参考轮廓。不同定义应分别命名。
4. 在同一物理频带计算左右边缘、宽度、中心线 PSD 与交叉谱。
5. 估计测量噪声及其不确定度，给出带偏差的原始量与经过验证的校正量。
6. 输出测量长度、采样间距、频带、噪声模型、有效数据量和置信区间。

可考虑拟合 `S(f)=A/[1+(2πfξ)²]^(H+1/2)+N(f)` 一类自仿射谱模型，估计相关长度 ξ 和指数 H；这只是候选模型，只有测量范围覆盖相应谱段且残差支持时才应报告参数。不能在几十个不连续点上强行得到有意义的 ξ/H。

本实现的 P 使用 SciPy 单边 density 谱；去偏时必须与该归一化约定一致。不要直接复制采用另一种单/双边定义的论文常数。以受控信号的时域方差与 `sum(PSD) × df` 做单元校验是合适的；本次 boxcar/periodogram 的独立测试得到 2.0 与 1.9999999999999996 nm²。

对经过滤波的数据，一般需要考虑类似 `S_observed(f)=|H_filter(f)|² S_sample(f)+S_noise(f)` 的关系。传递函数接近零的频带无法稳定反演；不能把已被 32 行平均完全抹掉的频率靠后处理“恢复成准确粗糙度”。这是结合代码滤波结构提出的工程判断。

### 5.3 独立对照工具：SMILE

SMILE 是面向 SEM line/space 和 contact 图像的公开量测工具，提供 LER/LWR、相关长度等分析，可作为独立实现的对照来源。它也有需要确认的参数和实验性功能，不能当作无需验证的绝对真值。[SMILE 作者项目页面](https://i-mochi.com/SMILE.html)

建议选同一批原始图，明确像素尺寸、ROI、边缘定义、去趋势、频带和噪声处理后再比较。先比较边缘轨迹和 PSD，再比较标量；两软件 3σ 接近但用了不同滤波，不足以证明一致性。本次未安装运行 SMILE，没有声称 V13 与它已有量化性能差距。

### 5.4 多尺度诊断和机器学习的位置

波形局部异常、周期噪声和非平稳性可用多尺度方法辅助定位。NIST 的小波研究提供空间与频率同时定位的分析思路，可用于区分局部缺陷与仪器效应。[NIST：Spectral Analysis of Line Edge and Line Width Roughness Using Wavelets](https://www.nist.gov/publications/spectral-analysis-line-edge-and-line-width-roughness-using-wavelets)

深度学习去噪已有 SEM 粗糙度研究，但应验证对 CD、PSD 和去偏粗糙度的影响，不能仅以图像“更干净”评价。对当前项目，确定性漏检和量测定义比先换深度模型更值得优先处理。[原始研究：Deep Learning Denoiser Assisted Roughness Measurements…Analysis with SMILE](https://arxiv.org/abs/2310.14815)

## 6. 建议的实施顺序与验收条件

以下为建议方案，本次没有实施包内算法修改。

| 阶段 | 工作 | 可检查的完成条件 |
|---|---|---|
| A：修复明确缺陷 | 重构阈值交点搜索；拒绝单点/单组粗糙度；质量门槛逐指标传播 | 阈值内外侧交点均可检出；支持不足返回 NaN；REVIEW 不混入正式均值 |
| B：建立统一统计 | 同引擎/掩码/频带输出；同时保留 raw 和 groupN；统一 PSD 支持策略 | 协方差恒等式在一致定义下成立；统计/PSD 差异有明确来源 |
| C：保留粗糙度带宽 | 分离粗定位与细采样；控制平均窗口和路径惩罚 | 已知波长/幅值合成曲线的增益、偏差和失效范围有报告 |
| D：噪声与不确定度 | 重复图像、独立噪声估计、PSD 去偏、分层置信区间 | 改变噪声/帧数时，校正结果在估计不确定度内稳定 |
| E：绝对 CD 标定 | 比较阈值、ERF、需要时的物理模型，建立尺度和边缘偏差校准 | 留出参考样品验证 bias 和重复性；不靠同一数据调参与验收 |
| F：批处理交付 | 依赖锁定、哈希、分块输出、每指标可追溯 | 同一输入/版本/配方可复现，失败和部分结果被下游正确识别 |

建议新增的正式输出信息包括：

```text
CD_mean_nm, LER_left_raw_3sigma_nm, LER_right_raw_3sigma_nm
LWR_raw_3sigma_nm, LWR_groupN_3sigma_nm
band_limited_LER/LWR, noise_estimate, unbias_status
valid_pair_count, valid_group_count, continuous_length_nm
synthetic_count, recovered_count, selected_object_ids
pixel_size_x_nm, pixel_size_y_nm, calibration_source
detrend_policy, frequency_band, filter_parameters
metric_qc_status, uncertainty_or_CI, input_hash, code_hash
```

这里“CI/不确定度”不能简单用重叠的 128 点当独立样本计算。应按数据生成过程采用独立图像/结构层级重采样，或经过验证的空间块 bootstrap，并单独纳入标定和模型偏差。

## 7. 验证数据应该如何补齐

目前自检主要证明公式实现与规定行为一致，不足以证明 SEM 量测准确。建议建立以下分层数据集：

| 数据层 | 建议内容 | 检验目标 |
|---|---|---|
| 解析边缘坐标 | 平行倾斜直线、恒宽共同摆动、左右独立/相关粗糙度、缺点、单组 | 几何、协方差关系、支持条件 |
| 合成灰度图 | 已知宽度/频谱；像素相位、模糊、噪声、对比度、非对称边缘、倾角扫描 | 检测偏差、失效率和频率响应 |
| 更真实的 SEM 响应仿真 | 与适用材料/电压/结构匹配的线扫模型，包含干扰 | 避免只在 ERF 合成图上验证 ERF |
| 真实 SEM 重复图 | 同位置重复、不同帧数/剂量、不同位置和图像场 | 重复性、漂移、噪声相关及采样变异 |
| 参考样品/独立方法 | 已知 pitch、适用的尺寸参考、必要时截面/AFM 等 | 像素比例与物理 CD 偏差 |

剂量扫描需要考虑样品收缩或损伤，不能假设不同剂量下样品几何完全不变。验收容差应由目标线宽、工艺窗口、设备能力和测量用途确定；本报告不凭空给出通用“误差小于某 nm 即合格”的门槛。

## 8. 已通过的检查与其边界

- 两项包内自检通过：解析坐标/分组/标签，以及单引擎异常、无候选、部分结果、标注失败等情形。
- trench 与 line 完整示例可导出主表、PSD 工作簿和边缘标注，诊断错误表为空；但两图均为 REVIEW，且 V13 的 PSD 均因数据支持不足跳过。
- 明暗反相路径一致性正确。
- 主流程共同有效掩码正确。
- 平行直边的 PCA 旋转与 cosθ 宽度投影公式正确。
- 在限定 boxcar/periodogram 测试中，PSD 密度归一化和积分正确。
- missing slots 在 PSD 中保留，采用连续段处理，没有把缺口压缩后直接 FFT，也没有把不同图片拼成一条轨迹。
- 单图辅助输出异常不会立刻丢弃已计算指标；最终整批导出步骤仍需针对大批量或写入故障单独验证。

**已经通过的检查不包括**真实物理 CD 溯源、unbiased LER/LWR、任意倾角/曲线量测、所有可选 Viterbi/ERF 组合或大规模批处理压力测试。

## 9. 复现方法

在仓库根目录解压原包并安装其声明依赖后运行：

```bash
busybox unzip -n sem_cd_measure_200k_batch_V13_modified_complete_package.zip
busybox unzip -n sem_cd_measure_200k_batch_V1_13_complete_package.zip
python3 -m venv .venv
.venv/bin/python -m pip install -r sem_cd_measure_200k_batch_V13_modified_complete_package/requirements_V13_modified.txt
.venv/bin/python audits/reproduce_v13_review.py --output /tmp/v13_review_evidence.json
```

若系统安装了独立 unzip，可把 `busybox unzip` 替换为 `unzip`。复现脚本使用临时目录运行样例并自动清理临时导出；写出的 JSON 保留数值证据。向外扩展搜索仅在审查 Python 进程中替换函数，并在 `finally` 中恢复；结束时再次检查原包文件哈希。

本次审查环境与完整依赖版本见 [证据 JSON](audits/v13_review_evidence.json)。原 requirements 未锁版本，其他环境若产生差异，应先比较依赖、输入和源码哈希，再分析算法差异。

实际使用建议是：先按阶段 A 修复已确认问题，再把 CD 和粗糙度的量测定义固定下来，用阶段 B–E 的证据逐步建立可信度。现有架构的候选定位、逐点诊断、双坐标输出和错误保留机制都可继续使用，无需为专业化而立即整体重写。
