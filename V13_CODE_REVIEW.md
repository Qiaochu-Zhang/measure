# V13_modified：CD、LER、LWR 代码审查报告

审查日期：2026-09-18
审查对象：`sem_cd_measure_200k_batch_V13_modified_complete_package`
目标：评估该程序是否适合测量 CD、左/右 LER 和 LWR。

## 结论

程序能够运行，也确实能够输出 CD、左右 LER 和 LWR。其语法检查、包内自检以及随包 trench/line 示例均可执行通过；旋转坐标、结果标签和异常记录机制也基本完整。

但是，目前版本更适合作为研发和算法对比工具。未经真实 SEM 数据标定、重复性研究及机台对照验证，不建议直接将输出作为正式量测结果。正式使用前最需要解决的是：低质量结构仍参与汇总、mixed 指标来自两个不同边缘引擎，以及 LWR 分组时左右边缘可能没有使用同一批有效采样点。

## 主要问题

### 1. 高风险：质量不合格的数据仍然输出正式数值

随包的 trench 和 line 示例中，V13 每条结构只有约 33%–35% 的采样点有效，远低于代码设置的 70% 最低要求，但程序仍通过 fallback 汇总并输出 CD、LER 和 LWR，只把图片状态标为 `REVIEW`。

相关逻辑：

- `sem_cd_measure_200k_batch_V13_modified.py:285`：`run_coordinate_engine()`
- `sem_cd_measure_200k_batch_V13_modified.py:333`：`primary_from_engines()`

实际示例结果：

- V13 有效点：每条结构 42–45/128；
- 最低要求：约 90/128；
- 图片状态：`REVIEW`；
- 仍输出 mixed CD = 59.8194 nm、mixed LWR = 0.7312 nm。

如果后续程序只读取 Excel 中的数值而忽略 `status`，低可信度结果很容易被当成合格结果。

建议：

- 默认不允许 unstable 结构进入正式 image-level 汇总；
- 将此类数值放入明确的 diagnostic/fallback 列；
- 正式结果列留空，除非满足最低有效点比例和最低稳定结构数；
- 在 CSV/Excel 中增加明确的 `usable_for_production` 布尔字段。

### 2. 高风险：mixed 结果来自两个不同边缘检测引擎

当前最左侧 mixed 结果定义为：

- CD、LWR：来自 V13；
- 左 LER、右 LER：来自 V10。

相关代码：`sem_cd_measure_200k_batch_V13_modified.py:1241`。

这四个指标不是从同一组边缘轨迹计算，因此不是严格自洽的一套测量。随包示例中：

- V13 CD：59.8194 nm；
- V10 CD：57.6449 nm；
- 两者相差约 2.17 nm。

这个差异说明两个引擎识别出的边缘位置并不相同。在这种情况下，将 V13 的宽度统计和 V10 的边缘粗糙度拼成一组主结果，会破坏 CD、LER、LWR 之间本应存在的统计关系。

建议：

- 正式主结果选择单一引擎统一计算 CD、左右 LER 和 LWR；
- mixed 仅作为历史兼容、机台拟合或算法对照结果；
- mixed 列名和说明中明确写出“跨引擎组合，不保证统计自洽”。

### 3. 高风险：LWR 分组可能使用不匹配的左右采样点

当前实现先分别对左边缘和右边缘执行分组 `nanmean`，再使用两组均值计算宽度：

- `sem_cd_measure_200k_batch_V13_modified.py:530`
- `cdsem_batch_v7_full_pipeline.py:710`

如果某一组内左边缘只在采样点 A、B 有效，而右边缘只在 C、D 有效，程序仍会用两组不同空间位置的平均值相减，产生一个并不对应真实局部宽度的数值。这可能制造或压低 LWR。

建议改为：

1. 先建立左右边缘共同有效掩码；
2. 在每个共同有效采样点计算 local CD；
3. 对 local CD 按原始采样槽位分组；
4. 仅用满足最低共同有效点数的组计算 LWR 3σ；
5. 输出每组共同有效点数，便于审计。

### 4. 像素标定完全依赖手工输入

程序对整个输入目录使用同一个 `--pixel-size`，默认值为 1.3181 nm/pixel。若图片倍率、扫描尺寸、裁剪方式或导出配置不同，CD、LER、LWR 会按相同比例整体出错。

不能仅凭“200K”倍率使用默认值。正式量测至少应做到：

- 从 SEM 元数据或图中标尺读取实际 nm/pixel；
- 每张图片记录标定值及其来源；
- 检查同一输入批次是否混入不同分辨率、倍率或扫描宽度；
- 使用已知 pitch/CD 的标准样片验证比例；
- 若 X/Y 像素比例可能不同，应分别支持 X、Y 标定。

### 5. LER/LWR 的空间带宽定义不充分

默认设置在每个采样位置平均 32 行，并使用 128 个采样点。相邻平均窗口可能高度重叠，这会形成空间低通滤波并压低高频粗糙度。

当前主结果没有完整报告：

- 实际量测长度；
- 有效采样间距；
- 平均窗口对应的物理长度；
- 窗口重叠率；
- LER/LWR 的有效空间频率范围。

因此，不同图像尺寸、ROI、采样数或 `average-range` 设置下的 LER/LWR 不应直接比较。建议在结果中保存这些物理参数，并固定一套量测带宽规范。

### 6. 随包验证不足以证明真实 SEM 量测能力

随包 line 和 trench 图片是合成示例，不是真实 SEM 数据。两个示例文件虽然不同，但实际运行得到完全相同的六组指标。这能说明亮/暗极性处理路径可以工作，但不能证明算法能可靠应对真实 SEM 中的：

- charging 和亮度漂移；
- shot noise、模糊和低对比度；
- 材料差异和非对称边缘过渡；
- 线端、桥连、断线、污染和局部缺陷；
- 扫描畸变和图像非线性；
- 不同旋转角度、pitch 和 duty cycle。

建议增加以下验证：

- 已知 CD、LER、LWR 真值的合成图扫描；
- 不同噪声、模糊、对比度和旋转角度的参数扫描；
- 同位置重复拍摄的 repeatability；
- 不同日期、设备和操作者的 reproducibility；
- 与正式 CD-SEM 机台结果的 bias、线性、相关性和残差分析；
- 标准样片上的 Gage R&R 或等价量测系统分析。

## 已通过的检查

- 两个 ZIP 已分别完整解压，原 ZIP 保留；
- 包内所有 Python 文件语法编译通过；
- `self_check_V13_modified.py` 通过；
- `self_check_partial_results.py` 全部通过；
- trench 和 line 示例都能生成 Excel/CSV；
- 24 个旋转/未旋转结果列的顺序和标签正确；
- 旋转坐标使用中心线 PCA/正交拟合，基本几何关系正确；
- CD 使用均值，LER/LWR 使用 3σ；
- 当前默认标准差定义为总体标准差 `ddof=0`；
- 输入失败、单引擎失败和辅助输出失败有异常记录机制；
- 部分结果不会因为绘图或 PSD 导出失败而全部丢失。

## 实际示例运行结果

使用参数：

```text
--pixel-size 1
--trench-reference-nm 60 / --line-reference-nm 60
--max-number 4
--no-auto-machine-comparison
--no-annotated-images
--skip-statistics-plots
--skip-psd
```

trench 和 line 示例均输出：

| 结果组 | CD (nm) | 左 LER (nm) | 右 LER (nm) | LWR (nm) |
|---|---:|---:|---:|---:|
| 旋转 mixed | 59.8194 | 2.2949 | 2.3072 | 0.7312 |
| 旋转 V13 | 59.8194 | 2.0537 | 2.1056 | 0.7312 |
| 旋转 V10 | 57.6449 | 2.2949 | 2.3072 | 0.7019 |
| 未旋转 mixed | 59.8194 | 2.3007 | 2.3154 | 0.7312 |
| 未旋转 V13 | 59.8194 | 2.0611 | 2.1066 | 0.7312 |
| 未旋转 V10 | 57.6449 | 2.3007 | 2.3154 | 0.7019 |

两个示例最终状态均为 `REVIEW`，原因是 V13 稳定结构数量为 0，单条结构有效比例只有约 0.328–0.352，低于 0.700 的要求。

## 修改优先级建议

### P0：正式使用前必须处理

1. unstable 结构默认不得进入正式 image-level 数值；
2. 修复 LWR 分组，使左右边缘只使用共同有效采样点；
3. 正式主结果改为同一引擎统一计算 CD/LER/LWR；
4. 强制确认或自动读取每张图的像素标定。

### P1：建立可信量测体系

1. 明确 LER/LWR 的量测长度和空间带宽；
2. 增加真实 SEM 数据集和标准样片验证；
3. 对默认参数进行敏感性扫描；
4. 建立与机台的 bias、repeatability 和 reproducibility 报告。

### P2：可维护性和输出改进

1. 把 fallback 数值与 production 数值分列；
2. 输出每个 LWR 分组的共同有效点数；
3. 在主表中增加 `usable_for_production` 和明确的失败原因代码；
4. 为真实缺点、人工补点和插值点分别统计数量；
5. 将核心统计公式写成独立、可单元测试的模块。

## 最终判断

V13_modified 已具备可运行的 CD/LER/LWR 分析流程和较丰富的诊断输出，但当前默认降级策略会让低质量测量仍以数值形式出现，mixed 指标也不是一套统计自洽的边缘结果。完成 P0 修改并通过真实 SEM 数据的量测系统验证之前，应将其输出视为算法评估结果，而不是正式生产测量值。
